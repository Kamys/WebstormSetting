import React from 'react';
import PropTypes from 'prop-types';

function ${NAME}() {
    return (
        <div>
            Component ${NAME}
        </div>
    );
}

${NAME}.propTypes = {};

${NAME}.defaultProps = {};

export default ${NAME};