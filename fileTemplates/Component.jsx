import React from 'react';
import PropTypes from 'prop-types';

class ${NAME} extends React.Component {
    state = {
    
    }

    render() {
        return (
            <div>
                Component ${NAME}
            </div>
        );
    }
}

${NAME}.propTypes = {};

${NAME}.defaultProps = {};

export default ${NAME};